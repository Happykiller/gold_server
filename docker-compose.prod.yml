# docker-compose.prod.yml
services:
  gold_back:
    container_name: gold_back
    image: gold_back
    restart: always
    networks:
      - interservices

networks:
  interservices:
    external: true