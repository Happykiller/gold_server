# docker-compose.prod.yml
services:
  gold_back:
    container_name: gold_back
    image: gold_back
    ports:
      - '3001:3000'  
    restart: always
    extra_hosts:
      - 'host.docker.internal:host-gateway'